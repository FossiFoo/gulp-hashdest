{
    "author": {
        "email": "fossi@iname.com",
        "name": "Fossi",
        "url": ""
    },
    "dependencies": {
        "gulp-util": "*",
        "through2": "*",
        "lodash": "*"
    },
    "description": "put files in a directory named based on the hash of it's content",
    "devDependencies": {
        "coveralls": "*",
        "event-stream": "*",
        "gulp": "*",
        "istanbul": "*",
        "mocha": "*",
        "mocha-lcov-reporter": "*",
        "should": "~2.1.0"
    },
    "engines": {
        "node": ">=0.8.0",
        "npm": ">=1.2.10"
    },
    "keywords": [
        "gulpplugin"
    ],
    "license": "MIT",
    "name": "hashdest",
    "repository": "",
    "scripts": {
        "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
        "test": "istanbul test _mocha --report html -- test/*.js --reporter spec"
    },
    "version": "0.0.1"
}
